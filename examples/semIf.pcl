program hello;
    var x : integer;

    (*function test (y : integer; z : integer) : integer;
        procedure test2 (k : integer);
        begin
            test (5, 6);
            test2(5);
        end;
    begin
        test2 (5);
        result := y + z + x;
    end;*)

begin
    x := 1;
    
    if x <> 1 then
    begin
        if x <> 1 then
            x := 4
        else
            x := 5;
    end;

    x := 5;

    if x <= 5 then
        x := x + 1;
end.