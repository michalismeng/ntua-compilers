program hello;
    var x : integer;

    function test (y : integer; z : integer) : integer;
        procedure test2 (k : integer);
        begin
            y := k;
        end;
    begin
        test2 (5);
        result := y + z + x;
    end;

begin
    x := 1;
    x := test (100, 1);
    x := x + 1;
end.