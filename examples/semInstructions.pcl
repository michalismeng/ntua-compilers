program hello;
    var x : integer;

    function test (y : integer; z : integer) : integer;
        function test2 (k : boolean) : boolean;
        begin
        end;
    begin
        y := z + 1;
    end;

begin
    x := 2;
    x := x + 1;
end.