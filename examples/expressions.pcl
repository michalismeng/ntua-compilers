program hello;

begin
    x := (1 + 2 - 2 * 3) / 3;        (* -1 *)
    y := @x * 2;                     (* 2440 = (1000 + 100 + 120) * 2 *)
    z := x[10] / 2 + 1;              (* 256 *)
    w := @x[10];                     (* 1510 *)
    a := @x^;                        (* 4880 *)
    u := @a[10]^;
    z := @a^[10];                    (* ?? *)
    o := x[1 + a^];
end.