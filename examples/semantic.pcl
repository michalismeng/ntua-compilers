program hello;

begin
    s := @"asd"[2];
    i := ((1 + 1) div 2) mod 4;
    r := 2.0;
    r := 1 + .;
    i := +1 - -1;
    b := not true;
    c := @"hello"^[2];
    "hello" := "hd";            (* This shouldn't work *)
    b := (1 * 2 < 2 + 2) and (2 <= 3) or (2 < 3)
end.